apiVersion: v1
kind: Service
metadata:
  name: {{ include "jtl-demo.fullname" . }}
  labels:
    {{- include "jtl-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jtl-demo.selectorLabels" . | nindent 4 }}
