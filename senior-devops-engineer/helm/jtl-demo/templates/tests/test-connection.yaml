apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jtl-demo.fullname" . }}-test-connection"
  labels:
    {{- include "jtl-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jtl-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
